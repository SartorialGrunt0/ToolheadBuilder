---
import { loadMetadata } from "../data/loadMetadata";
import CompatibilityTable from "./CompatibilityTable.astro";

const { hotends } = await loadMetadata();
---

<CompatibilityTable items={hotends} type="extruder" />
